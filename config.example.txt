; Arquivo de configuração do jogo de votação
; Copie este arquivo para config.txt e preencha os valores.
; Não use aspas, apenas texto simples.

; Chave da API do YouTube Data v3 (modo API key)
API_KEY=YOUR_API_KEY_HERE

; Token de acesso OAuth 2.0 (opcional)
ACCESS_TOKEN=

; ID do canal do YouTube que está fazendo a live
CHANNEL_ID=YOUR_CHANNEL_ID_HERE

; ID do chat ao vivo da sua live (liveChatId).
; AUTO = descobre automaticamente via API
LIVE_CHAT_ID=AUTO

; Intervalo de atualização em milissegundos (10000 = 10 segundos)
REFRESH_INTERVAL=5000

; ---------- Configurações da votação ----------
NOME_VOTACAO=My Poll Title
ELEMENTOS_VOTACAO=Option1,Option2,Option3
OPTION_IMAGES=
OPTION_COMMANDS=1,2,3
MAX_VOTES_PER_USER=1

; ---------- Configurações da picareta ----------
PICKAXE_CHANGE_INTERVAL=10

; Nome do dono do canal (jogador principal)
OWNER_NAME=@your_username
